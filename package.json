{
  "name": "habittracker",
  "version": "1.0.0",
  "description": "Habit tracker application with tests, client and server",
  "main": "",
  "directories": {
    "doc": "docs",
    "test": "tests",
    "client": "client",
    "server": "server"
  },
  "scripts": {
    "test": "cd tests && npm test",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "all": "npm run client & npm run server",
    "install": "cd client && npm install && cd ../server && npm install && cd ../tests && npm install"
  },
  "author": "Ekipa brez imena",
  "license": "ISC"
}
