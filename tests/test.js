require('./server/test_auth')
require('./server/test_tasks')
require('./server/test_activities')

describe('Main Test Suite', () => {
    it('should run all tests', () => {
        console.log('All tests are running');
    });
});
